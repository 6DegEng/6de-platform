streamlit>=1.47,<2
pandas>=2.3,<3
openpyxl>=3.1,<4
bcrypt>=4.0,<6
pyyaml>=6.0,<7
streamlit-authenticator>=0.4,<1
pytest>=8.0,<9

# Phase 8 — persistent Postgres backend (DB_BACKEND=postgres)
psycopg[binary]>=3.2,<4

# Phase 2 — SharePoint document layer via Microsoft Graph
msgraph-sdk>=1.0,<2
azure-identity>=1.15,<2
cryptography>=46.0,<47
python-dotenv>=1.0,<2

# Session 3a — Projects page UI uplift (Table / Kanban / Timeline / Calendar)
streamlit-aggrid>=1.2,<2
streamlit-sortables>=0.3,<1
streamlit-calendar>=1.4,<2
plotly>=5.0,<7
